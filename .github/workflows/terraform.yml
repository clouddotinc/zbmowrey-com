on:
  pull_request:
    paths:
      - 'terraform/**'
    branches:
      - develop
      - staging
      - main
  push:
    paths:
      - 'terraform/**'
    branches:
      - develop
      - staging
      - main
jobs:
  terraform_cycle:
   uses: zbmowrey/deploy-terraform/.github/workflows/terraform.yml@main